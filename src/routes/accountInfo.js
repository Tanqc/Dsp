import React from 'react';
import { connect } from 'dva';
import AccountInfoComponent from '../components/account/accountInfo';

function AccountInfo({ location }) {
  return (
    <AccountInfoComponent />
  )
}

export default AccountInfo;
